apiVersion: v1
kind: Service
metadata:
  name: load-balancer
  namespace: default
spec:
  selector:
    app: hello-node-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
